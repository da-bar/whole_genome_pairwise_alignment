process {
    // setup step of the real test; keeps its output name distinct from UCSC_CHAINSTITCHID's
    withName: UCSC_NETCHAINSUBSET {
        ext.args   = '-verbose=0'
        ext.prefix = { "${meta.id}.subset" }
    }
}
