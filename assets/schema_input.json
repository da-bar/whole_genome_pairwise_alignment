{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/da-bar/wgpa/master/assets/schema_input.json",
    "title": "da-bar/wgpa pipeline - params.input schema",
    "description": "Schema for the file provided with params.input: one row per genome pair (target and query FASTA)",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^[^\\s/]+$",
                "errorMessage": "Pair id must be provided and cannot contain spaces or '/'",
                "meta": ["id"]
            },
            "target": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(fa|fasta|fna)(\\.gz)?$",
                "errorMessage": "Target FASTA must be provided, cannot contain spaces and must have extension '.fa', '.fasta' or '.fna', optionally followed by '.gz'"
            },
            "query": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(fa|fasta|fna)(\\.gz)?$",
                "errorMessage": "Query FASTA must be provided, cannot contain spaces and must have extension '.fa', '.fasta' or '.fna', optionally followed by '.gz'"
            },
            "preset": {
                "type": "string",
                "enum": ["near", "medium", "far"],
                "errorMessage": "Preset must be one of 'near', 'medium' or 'far', or left empty to use --preset",
                "meta": ["preset"]
            }
        },
        "required": ["id", "target", "query"]
    },
    "uniqueEntries": ["id"]
}
